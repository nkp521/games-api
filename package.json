{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Simple JSON server for Render",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
